throw new Error('Pretext ASCII renderer disabled; use the stable monospace ASCII renderer.');

export function prepareWithSegments(value, font) {
  return {
    segments: Array.from(String(value)),
    widths: []
  };
}
